function add (a, b) {
    return a + b;
}

function sub (x, y) {
    return x - y;
}

function mul (a, b) {
    return a * b
}

module.exports = {
    add,
    sub
}   // per file only one default export, and as many as named exports you want